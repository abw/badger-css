import { Snippet } from '@abw/badger-website'

# Tabs

Add the `tabset` class to a container and then define a set of elements with
the `tab` class.  The `disabled` class can be added to a tab to disable it.

<Snippet
  file="tabs/tabs.html"
  caption="Tabs"
  output
/>

## Lined

Add the `lined` class to the `tabset` to add a line under the tabs.

<Snippet
  file="tabs/lined.html"
  caption="Lined Tabs"
  output
/>

## Solid

Add the `solid` class to the `tabset` for solid tabs.

<Snippet
  file="tabs/solid.html"
  caption="Solid Tabs"
  output
/>

## Center

Add the `center` class to the `tabset` to align the tabs in the center.

<Snippet
  file="tabs/center.html"
  caption="Center Tabs"
  output
/>

## Right

Add the `right` class to the `tabset` to align the tabs to the right.

<Snippet
  file="tabs/right.html"
  caption="Right Tabs"
  output
/>

## Scroll

If there are too many tabs to be displayed then they will appear in a
horizontally scrolling element.

<Snippet
  file="tabs/scroll.html"
  caption="Scrolling Tabs"
  output
/>
