import { Snippet } from '@abw/badger-website'

# Tooltips

Add the `data-tooltip` attribute to an element to enable a tooltip.  The
tooltip text should be defined in the `aria-label` property so that it's also
accessible to screen readers.

<Snippet
  file="tooltip/tooltip.html"
  caption="Tooltip"
  output
/>

## Tooltip Position

You can set the `data-tooltip` attribute to any of `left`, `right`, `top` or
`bottom` to set the tooltip position.

<Snippet
  file="tooltip/position.html"
  caption="Tooltip Position"
  output
/>

You can also combine them as `top left`, `top right`, `bottom left` and
`bottom right`.

<Snippet
  file="tooltip/corner.html"
  caption="Tooltip Corners"
  output
/>

## Wrapping

The tooltip has a minimum and maximum width set to keep things sensible. You
can add the `tt-wide` class if you want the tooltip to extend to the full
width of the tooltip text.

You can add newlines to the `aria-label` property if you want to force line
breaks.

<Snippet
  file="tooltip/wrap.html"
  caption="Tooltip Wrapping"
  output
/>

## Content

As an alternative to setting the `aria-label` you can define the tooltip
content in a child element with the `tooltip` CSS class.

<Snippet
  file="tooltip/content.html"
  caption="Tooltip Content"
  output
/>

## Custom Tooltip Style

There are a number of SCSS variables that can be used to set the defaults, or
you can use CSS variables to create custom tooltip styles.

<Snippet
  file="tooltip/custom.html"
  caption="Custom Tooltips"
  output
/>
