import { Snippet, Link } from '@abw/badger-website'

# Prefix and Suffix

You can add prefixes and suffixes to form fields, or any combination of
<Link to="/forms/multi-inputs" text="inputs"/>. Not only do the colours and
focus rings work correctly, but they also adapt their colors for `valid` and
`invalid` fields.

<Snippet
  file="prefix-suffix/fixes.html"
  caption="Prefix and Suffix"
  output
/>

## Shaded

To visually distinguish a prefix or suffix you you can add the `shaded`
class...

<Snippet
  file="prefix-suffix/shaded.html"
  caption="Shaded"
  output
/>

## Lined

...or the `lined` class.

<Snippet
  file="prefix-suffix/lined.html"
  caption="Lined"
  output
/>

## Shaded and Lined

...or both.

<Snippet
  file="prefix-suffix/shaded-lined.html"
  caption="Shaded and Lined"
  output
/>

## Input Prefix

If you want to add a prefix to a field you should enclose the input in an
element with the `inputs` class and add the `prefix` class to the prefix
element.  You can add a prefix to a text input, textarea, select or a
checkbox or radio option with the `border` class set.  You can try it without
the `border` but the effect is lost somewhat.

### Text Input With Prefix

<Snippet
  file="prefix/text.html"
  caption="Text Prefix"
  output
/>

### Text Area Input With Prefix

<Snippet
  file="prefix/textarea.html"
  caption="Text Area Prefix"
  output
/>

### Select Input With Prefix

<Snippet
  file="prefix/select.html"
  caption="Select Prefix"
  output
/>

### Checkbox With Prefix

This only really works with checkboxes that have the `border` class

<Snippet
  file="prefix/checkbox.html"
  caption="Checkbox Prefix"
  output
/>

### Fake Input

Instead of an input you can add the `input` CSS class to any element.

<Snippet
  file="prefix/fake.html"
  caption="Fake Input Prefix"
  output
/>

## Input Suffix

Adding an input suffix is just the same, except for giving your suffix element
the `suffix` class and putting it after the input.

### Text Input With Suffix

<Snippet
  file="suffix/text.html"
  caption="Text Suffix"
  output
/>

### Text Area Input With Suffix

<Snippet
  file="suffix/textarea.html"
  caption="Text Area Suffix"
  output
/>

### Select Input With Suffix

<Snippet
  file="suffix/select.html"
  caption="Select Suffix"
  output
/>

### Checkbox With Suffix

This only really works with checkboxes that have the `border` class

<Snippet
  file="suffix/checkbox.html"
  caption="Checkbox Suffix"
  output
/>
