import { Snippet } from '@abw/badger-website'

# Flex

## Flex Classes

Use the `flex` CSS class to set the `display` mode to `flex`.  The default
alignment mode (i.e. the `align-items` CSS property) is `stretch`.

<Snippet
  file="flex/flex.html"
  caption=".flex"
  expand output
/>

## Gaps

The `gap-N` classes can be used to add a gap between successive elements.
Here `N` is a number from 1 to 16 representing multiples of the default
spacing unit (`0.25rem` by default).

<Snippet
  file="flex/gap.html"
  caption=".flex.gap-4"
  expand output
/>

## Flex Top

Add the `top` (or `start`) class to align the items to the stop.

<Snippet
  file="flex/top.html"
  caption=".flex.top"
  expand output
/>

## Flex Middle

Add the `middle` class to align the items to the middle.

<Snippet
  file="flex/middle.html"
  caption=".flex.middle"
  expand output
/>

## Flex Baseline

Add the `baseline` class to align the items to the text baseline.

<Snippet
  file="flex/baseline.html"
  caption=".flex.baseline"
  expand output
/>

## Flex Bottom

Add the `bottom` (or `end`) class to align the items to the bottom.

<Snippet
  file="flex/bottom.html"
  caption=".flex.bottom"
  expand output
/>

## Flex Left

Items are align to the left (technically, `flex-start`) by default but you can
add the `left` class if you want to make it explicit.

<Snippet
  file="flex/left.html"
  caption=".flex.left"
  expand output
/>

## Flex Center

Add the `center` class to align the items to the center.

<Snippet
  file="flex/center.html"
  caption=".flex.center"
  expand output
/>

## Flex Right

Add the `right` class to align the items to the right.

<Snippet
  file="flex/right.html"
  caption=".flex.right"
  expand output
/>

## Flex Space

Add the `space` class to space the items as much as possible. This is
equivalent to setting the `justify-content` CSS property to `space-between`.

<Snippet
  file="flex/space.html"
  caption=".flex.space"
  expand output
/>

## Flex Evenly

Add the `evenly` class to divide the space evenly between and around the
items.  This is equivalent to setting the `justify-content` CSS property to
`space-evenly`.

<Snippet
  file="flex/evenly.html"
  caption=".flex.evenly"
  expand output
/>

## Flex Wrap

Add the `wrap` class to set the `flex-wrap` property to `wrap`.

<Snippet
  file="flex/wrap.html"
  caption=".flex.wrap"
  expand output
/>
